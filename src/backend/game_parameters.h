#include "direction.h"

namespace GameParameters::Snake
{
    const Direction STARTING_DIRECTION = Direction::LEFT;
    const Coordinates STARTING_HEAD_COORDINATES{20, 10};
    const int STARTING_TOTAL_SEGMENT_COUNT = 5;
}
